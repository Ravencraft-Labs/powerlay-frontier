{
  "name": "@powerlay/electron-shell",
  "version": "0.1.0",
  "private": true,
  "main": "dist/main.js",
  "scripts": {
    "build": "pnpm --filter @powerlay/core build && tsc --noEmit && node esbuild.config.mjs",
    "dev": "pnpm run build && electron .",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "@powerlay/core": "workspace:*",
    "electron": "^28.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.3.0",
    "eslint": "^8.56.0",
    "@typescript-eslint/eslint-plugin": "^6.19.0",
    "@typescript-eslint/parser": "^6.19.0"
  }
}
